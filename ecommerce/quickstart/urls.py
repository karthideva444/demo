from django.urls import path
from . import views

urlpatterns = [
    # path('',views.customerview.as_view(),name="cust_view"),
    # path('get/<int:pk>',views.customerview.as_view(),name="view_data")
    ]
